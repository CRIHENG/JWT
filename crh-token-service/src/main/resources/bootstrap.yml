spring:
  application:
    name: token-service

server:
  port: ${service.server.port:12136}
  contextPath: /


eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.serviceUrl:http://localhost:12138/eureka/}